name: "Tests"
description: "Custom action for running tests"
inputs:
  param1:
    description: "First parameter for the test"
    required: true
  param2:
    description: "Second parameter for the test"
    required: true
runs:
  using: "node16"
  main: "dist/index.js"